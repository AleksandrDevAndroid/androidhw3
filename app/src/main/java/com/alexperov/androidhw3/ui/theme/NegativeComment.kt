package com.alexperov.androidhw3.ui.theme

import com.alexperov.androidhw3.Post

class NegativeComment(
    val ownerId: Int,
    val commentId : Int
){
    fun pushStrike(postId: Int,comment: Int) {

    }
}